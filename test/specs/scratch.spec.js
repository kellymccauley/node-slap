'use strict';
var should = require('should')
  , debug = require('debug')('slap:spec:scratch')
  , Q = require('q')
;

describe('Scratch Pad', function() {
  describe('Learning Q promises.', function() {
    it('Propagation', function() {
      // var outputPromise = getInputPromise()
      // 
      //   .then(
      //     // Fulfillment handler
      //     function(value) {
      //     },
      //     // Rejection handler
      //     function(err) {
      //     }
      //   )
      // 
      //   .then(function(value) {
      //   })
      // 
      //   // Like .then(null, function(reason) {})
      //   .fail(function(err) {
      //   });


    });

  });
});

